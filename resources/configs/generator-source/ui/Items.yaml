pages:
  items:
#    sizer:
#      cols: 1
#      rows: 7
#      growable_cols: [ 0 ]
#      growable_rows: [ 0, 1, 2, 3, 4, 5, 6 ]

    args:
      arg_name: "itemsArgs"
      args_in: [ "pageName", "std::string", "General",
                 "splitWindow", "bool", "false"
      ]
#
#    on_kill_active:
#      body: |
#        // Yadda yadda yadda
#           Interface::onKillActive(autoDisable);

    base_class: SplitterPage
    modules: ["SplitterPage", "Items.RS"]
    title: "Items"
    layout: "Items"
    elements:

    functions:
      select:
        return: "soci::rowset<>"
        override: True
        body: |
          auto rs = mc::ItemsRS(*ICtrl::db()->getTable("items"));
          return rs.getRowset("id = 1");

tables:
  items:
    fields:
      id:
        type: integer
        auto_increment: true
        primary_key: true
      bEnabled:
        type: hs_bool
        not_null: false
