pages:
  statistics:
#    sizer:
#      cols: 1
#      rows: 7
#      growable_cols: [ 0 ]
#      growable_rows: [ 0, 1, 2, 3, 4, 5, 6 ]

    args:
      arg_name: "statisticsArgs"
      args_in: [ "pageName", "std::string", "General",
                 "splitWindow", "bool", "false"
      ]
#
#    on_kill_active:
#      body: |
#        // Yadda yadda yadda
#           Interface::onKillActive(autoDisable);

    base_class: SplitterPage
    modules: ["SplitterPage", "Statistics.RS"]
    title: "Statistics"
    layout: "Statistics"
    elements:

    functions:
      select:
        override: True
        return: "soci::rowset<>"
        body: |
          auto rs = mc::StatisticsRS(*ICtrl::db()->getTable("statistics"));
          return rs.getRowset("id = 1");

tables:
  statistics:
    fields:
      id:
        type: integer
        auto_increment: true
        primary_key: true
      bEnabled:
        type: hs_bool
        not_null: false
